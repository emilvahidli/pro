# Database (used by Docker Compose and all services)
DB_USER=pro
DB_PASSWORD=your-secure-password
DB_NAME=proep
# DB_HOST/DB_PORT are set inside containers (postgres:5432); override only if needed
# DB_HOST=postgres
# DB_PORT=5432

# Telegram (optional – for proep.az backend contact form)
# TELEGRAM_TOKEN=
# TELEGRAM_CHAT_ID=

# Optional: admin panel scraping URL (if different from default)
# SCRAPING_SERVICE_URL=http://localhost:4000

# Timezone (optional)
# TZ=Asia/Baku
